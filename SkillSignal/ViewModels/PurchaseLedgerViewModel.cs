namespace SkillSignal.ViewModels
{
    public class PurchaseLedgerViewModel : PageViewModel
    {
        public PurchaseLedgerViewModel(INavigationService viewNavigationService) : base(viewNavigationService)
        {
            Title = "Purchase Ledger";
        }
    }
}